
#include "../include/cpu.h"

int main(void)
{
    system("cd && pause");

    cpu_work();

    //free(REG);
    //free(RAM);
    //free(VIDEO);
    //free(stk.data);
    //free(code);
}
